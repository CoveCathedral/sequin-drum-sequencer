"""Sequin's wxPython UI, and the accessibility helpers the whole app is built on.

Only three control types read reliably to NVDA here (checkbox, slider, dropdown), and
dense grids use a spoken tracker model — see ``accessibility`` and ``speech``.
"""
